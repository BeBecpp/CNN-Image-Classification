# CNN-Image-Classification
This project implements a Convolutional Neural Network (CNN) using PyTorch for image classification. The model is trained and evaluated on a dataset provided via Kaggle competition.  The goal is to build a simple but effective deep learning pipeline for classifying images into predefined categories.
